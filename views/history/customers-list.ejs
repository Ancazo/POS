<%- include('../partials/head') %>

<body>

    <div class="container">
        <table class="table table-striped">
            <tr>
                <th>Customer ID</th>
                <th>Customer Name</th>
                <th>Customer HP</th>
                <th>Created On</th>
                <th>Updated On</th>
            </tr>
            <tr>
                <% if (customers.length !== 0) { %> 
                    <% customers.forEach((element) => { %>
                <th><a href="/customers/<%= element._id %> "> <%= element._id %></a></th>
                <th><%= element.name %></th>
                <th><%= element.contact %></th>
                <th><%= element.createdAt.toDateString() %></th>
                <th><%= element.updatedAt.toDateString() %></th>
            </tr>
            <% }); %>
            <% } else { %>
                <p>No Customers List</p>
                <% } %>
        </table>

        <form method="GET" action="/sales">
            <button type="submit" class=" form-control btn-secondary">Back</a>
        </form>

    </div>
</body>
<%- include('../partials/foot') %>